version: "3"

vars:
  CONDA_ENV: ehub
includes:
  test: Taskfile.test.yml
  
tasks:
  run:
    desc: Run the ehub-core worker
    cmds:
      - PROJECT_ROOT={{.TASKFILE_DIR}} conda run --no-capture-output -n {{.CONDA_ENV}} python src/main.py

